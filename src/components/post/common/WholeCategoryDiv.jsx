/* eslint-disable react/prop-types */

const WholeCategoryDiv = ({children}) => {
  return (
    <div className="flex flex-col gap-y-4">
      {children}
    </div>
  )
}

export default WholeCategoryDiv
